Member 1 — Admin Dashboard & Product Engine

This folder contains only the files for Member 1's contribution (backend controllers, services, repositories, models, DTOs, WebConfig, and frontend Admin pages).

How to use

- The files are under `member1_only/`.
- To create a PR for only Member 1 changes, you can:
  1. Create a new branch in your git repo (e.g., `member1-main`).
  2. Copy the contents of `member1_only/` into your branch root (or replace the repo contents there).
  3. Commit and push the branch and open a pull request.

Notes

- I did not delete any original files; instead I copied Member 1 files into `member1_only/` to avoid destructive changes.
- If you want me to create a dedicated git branch and remove other files inside that branch instead, tell me and I will proceed (I'll need permission to modify repository files and will perform destructive actions only after you confirm).
